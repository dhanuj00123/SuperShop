import { ZoomimgDirective } from './zoomimg.directive';

describe('ZoomimgDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomimgDirective();
    expect(directive).toBeTruthy();
  });
});
